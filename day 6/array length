#include <stdio.h>

int main() {
   int arr[]={1,14,15,134,9,2,5,8,56,29};
   int max=0;
   int length =sizeof(arr)/sizeof(arr[0]);
   for(int i=0;i<=length-i;++i){
       if(max<arr[i])
       max=arr[i];
   }
   printf("%d",max);

    return 0;
}